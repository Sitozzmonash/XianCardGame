# 修仙卡牌后端：Python 3.11+，无深度学习依赖
# 安装：python -m venv .venv && .venv/Scripts/python.exe -m pip install -r requirements.txt

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
PyYAML>=6.0

# 测试
pytest>=8.0
pytest-timeout>=2.3
httpx>=0.27
