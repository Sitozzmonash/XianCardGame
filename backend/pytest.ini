[pytest]
testpaths = tests
pythonpath = .
addopts = -q
# 挂死保护：单条测试超过 10 分钟即判失败（CI 上避免无限挂起）。
# 重测试确实慢：test_training_parallel 会真实 spawn 多进程、test_training_legacy 要加载
# 参考实现产出的 124MB 旧模型，因此上限给得宽松，只用来兜住真正的死锁。
timeout = 600
timeout_method = thread
filterwarnings =
    ignore::DeprecationWarning
