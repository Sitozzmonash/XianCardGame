# Vercel Services: browser and FastAPI share one domain via root vercel.json.
NEXT_PUBLIC_API_BASE_URL=/api/v1
NEXT_PUBLIC_REQUEST_TIMEOUT_MS=30000
# 只有需要完全离线演示时才开启；生产环境必须保持 0。
NEXT_PUBLIC_USE_MOCK=0
