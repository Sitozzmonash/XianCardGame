/**
 * 首页专用子组件（参考 `components/screens/HomeScreen.tsx` 的逐块拆解）。
 *
 * - `HeroArt`：主角立绘 + `radial-gradient` 羽化遮罩（参考第 13–24 行）
 * - `HomeHero`：居中标题组 + 标题可读性遮罩（参考第 36–45 行）
 * - `TitleScrim`：标题遮罩本体（纯 SVG，按容器尺寸计算椭圆半径）
 */

export { HeroArt } from '@/components/ref/screens/home/HeroArt';
export { HomeHero } from '@/components/ref/screens/home/HomeHero';
export { TitleScrim } from '@/components/ref/screens/home/TitleScrim';
